#include <Validator.h>
#include <iostream>
#include <string>

class ExpresionEvaluator {
public:
  ExpresionEvaluator() {
    std::cout << "Expresion Evaluator creado" << std::endl;
    Validator hola;
  };

  void Evaluate(std::string expresion) {}
};